// Virtual-threads arena: @RunOnVirtualThread + blocking Hibernate ORM Panache.
//
// The exact same gRPC contracts and game logic as :mutiny-server, but the service
// methods run on virtual threads and the persistence is plain, blocking, imperative
// JDBC/Hibernate — no Uni/Multi chains, no reactive session. Java 21, encoding and
// -parameters come from the root build.
plugins {
    id 'io.quarkus'
    id 'idea'
}

dependencies {
    implementation enforcedPlatform("${quarkusPlatformGroupId}:${quarkusPlatformArtifactId}:${quarkusPlatformVersion}")

    // Shared: pure GameLogic + the proto contracts (scanned for gRPC codegen).
    implementation project(':common')

    // gRPC
    implementation 'io.quarkus:quarkus-grpc'

    // Blocking database: Hibernate ORM Panache over JDBC (NOT the reactive stack).
    implementation 'io.quarkus:quarkus-hibernate-orm-panache'
    implementation 'io.quarkus:quarkus-jdbc-postgresql'

    // Configuration & Logging
    implementation 'io.quarkus:quarkus-config-yaml'
    implementation 'io.quarkus:quarkus-logging-json'

    // Core
    implementation 'io.quarkus:quarkus-arc'

    // Testing
    testImplementation 'io.quarkus:quarkus-junit5'
}

test {
    systemProperty "java.util.logging.manager", "org.jboss.logmanager.LogManager"
}

idea {
    module {
        testSources.from(file('src/integrationTest/java'))

        def genProtoJava = file("$projectDir/build/generated/source/proto/main/java")
        def genProtoGrpc = file("$projectDir/build/generated/source/proto/main/grpc")
        def genAltGrpc = file("$projectDir/build/generated-sources/grpc")
        def genAltProtoJava = file("$projectDir/build/generated-sources/protobuf/java")

        [genProtoJava, genProtoGrpc, genAltGrpc, genAltProtoJava].each { d ->
            sourceDirs += d
            generatedSourceDirs += d
        }
    }
}
